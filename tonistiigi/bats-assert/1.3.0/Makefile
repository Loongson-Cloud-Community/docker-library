# This file is generated by the template.

REGISTRY	?=lcr.loongnix.cn
ORGANIZATION	?=tonistiigi
REPOSITORY	?=bats-assert
TAG		?=1.3.0
LATEST		?=true

IMAGE=$(REGISTRY)/$(ORGANIZATION)/$(REPOSITORY):$(TAG)
LATEST_IMAGE=$(REGISTRY)/$(ORGANIZATION)/$(REPOSITORY):latest
GIT_URL=https://github.com/Loongson-Cloud-Community/xx.git

default: image

src:
	git clone -b loong64-1.3.0-new $(GIT_URL) $@

image: src
	cd src/util/bats-assert &&  docker buildx bake --load -f docker-bake.hcl

push:
	docker tag  $(LATEST_IMAGE) $(IMAGE)
	docker push $(IMAGE)
	docker push $(LATEST_IMAGE)

clean:
	rm -rf src
