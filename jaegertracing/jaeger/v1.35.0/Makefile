# This file is generated by the template.

REGISTRY?=lcr.loongnix.cn
ORGANIZATION?=jaegertracing
REPOSITORY1?=jaeger-agent
REPOSITORY2?=jaeger-collector
REPOSITORY3?=jaeger-query
REPOSITORY4?=jaeger-ingester
REPOSITORY5?=jaeger-es-index-cleaner
REPOSITORY6?=jaeger-es-rollover
TAG?=v1.35.0

IMAGE1=$(REGISTRY)/$(ORGANIZATION)/$(REPOSITORY1):$(TAG)
IMAGE2=$(REGISTRY)/$(ORGANIZATION)/$(REPOSITORY2):$(TAG)
IMAGE3=$(REGISTRY)/$(ORGANIZATION)/$(REPOSITORY3):$(TAG)
IMAGE4=$(REGISTRY)/$(ORGANIZATION)/$(REPOSITORY4):$(TAG)
IMAGE5=$(REGISTRY)/$(ORGANIZATION)/$(REPOSITORY5):$(TAG)
IMAGE6=$(REGISTRY)/$(ORGANIZATION)/$(REPOSITORY6):$(TAG)

# SOURCE_URL is a url to download source, such as https://github.com/merore/merore.git.
# SOURCE is project sources, its located at src/$(SORUCE).
# PATCH is a patch that supports loong64 to $(SOURCE).
# Be sure to fill in the follows!!!
SOURCE_URL=https://github.com/jaegertracing/jaeger.git
SOURCE=$(shell echo $(SOURCE_URL) | awk -F '/' '{print $$NF}' | awk -F '.' '{print $$1}')
PATCH=0001-jaeger-v1.35.0-add-loong64.patch

default: image

image: src/$(SOURCE)
# Commands for building.
	$(MAKE) -C src/$(SOURCE) -f Makefile build-crossdock-ui-placeholder
	$(MAKE) -C src/$(SOURCE) -f Makefile build-binaries-loong64
	$(MAKE) -C src/$(SOURCE) -f Makefile docker-images-jaeger-backend
	$(MAKE) -C src/$(SOURCE) -f Makefile docker-images-elastic

src/$(SOURCE):
	git clone -b $(TAG) --depth=1 $(SOURCE_URL) $@
	cd $@ && \
		git apply ../../$(PATCH)
	

push:
	docker push $(IMAGE1)
	docker push $(IMAGE2)
	docker push $(IMAGE3)
	docker push $(IMAGE4)
	docker push $(IMAGE5)
	docker push $(IMAGE6)

clean:
	rm -rf src
