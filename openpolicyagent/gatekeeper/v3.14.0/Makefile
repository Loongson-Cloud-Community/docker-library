# This file is generated by the template.

REGISTRY?=lcr.loongnix.cn
ORGANIZATION?=openpolicyagent
REPOSITORY?=gatekeeper
TAG?=3.14.0

IMAGE=$(REGISTRY)/$(ORGANIZATION)/$(REPOSITORY):$(TAG)


default: image


pkg:
	wget https://cloud.loongnix.cn/releases/loongarch64/open-policy-agent/gatekeeper/3.14.0/gatekeeper-3.14.0-linux-loong64.tar.gz
	tar -xzf gatekeeper-3.14.0-linux-loong64.tar.gz
	chmod +x manager
	rm -rf gatekeeper-3.14.0-linux-loong64.tar.gz

download: pkg

image: download
	docker build \
		-t $(IMAGE) \
		.

push:
	docker push $(IMAGE)

