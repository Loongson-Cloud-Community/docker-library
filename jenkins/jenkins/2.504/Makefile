# This file is generated by the template.

REGISTRY?=lcr.loongnix.cn
ORGANIZATION?=jenkins
REPOSITORY?=jenkins
TAG?=2.504-jdk17

IMAGE=$(REGISTRY)/$(ORGANIZATION)/$(REPOSITORY):$(TAG)


default: image

image:
	docker build \
		--build-arg https_proxy=$(https_proxy) \
		-t $(IMAGE) \
		-f alpine/hotspot/Dockerfile \
		.

push:
	docker push $(IMAGE)
