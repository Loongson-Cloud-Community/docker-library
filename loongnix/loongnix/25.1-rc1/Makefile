# This file is generated by the template.

REGISTRY?=lcr.loongnix.cn
ORGANIZATION?=loongnix
REPOSITORY?=loongnix
TAG?=25.1-rc1

IMAGE=$(REGISTRY)/$(ORGANIZATION)/$(REPOSITORY):$(TAG)

ROOTFS=rootfs.tar.xz

default: image

image: $(ROOTFS)
	docker build \
		--build-arg ROOTFS=$(ROOTFS) \
		-t $(IMAGE) \
		.

push: 
	docker push $(IMAGE)

$(ROOTFS):
	docker run --rm \
		-v `pwd`:/work \
		lcr.loongnix.cn/library/debian:forky \
		/bin/bash -c "/work/make_rootfs.sh"

