# This file is generated by the template.

REGISTRY	?=lcr.loongnix.cn
ORGANIZATION	?=prometheus-community
REPOSITORY	?=postgres_exporter
TAG		?=0.17.1
LATEST		?=false

IMAGE=$(REGISTRY)/$(ORGANIZATION)/$(REPOSITORY):$(TAG)
LATEST_IMAGE=$(REGISTRY)/$(ORGANIZATION)/$(REPOSITORY):latest
SOURCE=postgres_exporter
SOURCE_URL=https://github.com/prometheus-community/postgres_exporter.git
PATCH=0001-err_fix.patch
default: image

image: src/$(SOURCE)
	export ARCH=loong64
	$(MAKE) -C src/$(SOURCE) build
	$(MAKE) -C src/$(SOURCE) common-docker
	docker tag prometheuscommunity/postgres-exporter-linux-loong64:HEAD $(IMAGE) 	
src/$(SOURCE):
	git clone -b v$(TAG) --depth=1 $(SOURCE_URL) $@
	cd $@ && git apply ../../$(PATCH)
clean:
	rm -rf src
push:
	docker push $(IMAGE)
	#latest image
	@if [ $(LATEST) = "true" ]; \
		then \
		docker tag $(IMAGE) $(LATEST_IMAGE); \
		docker push $(LATEST_IMAGE); \
	fi
