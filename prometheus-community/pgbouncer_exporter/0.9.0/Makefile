# This file is generated by the template.

REGISTRY	?=lcr.loongnix.cn
ORGANIZATION	?=prometheus-community
REPOSITORY	?=pgbouncer_exporter
TAG		?=0.9.0
LATEST		?=false

IMAGE=$(REGISTRY)/$(ORGANIZATION)/$(REPOSITORY):$(TAG)
LATEST_IMAGE=$(REGISTRY)/$(ORGANIZATION)/$(REPOSITORY):latest
SOURCE=pgbouncer_exporter
SOURCE_URL=https://github.com/prometheus-community/pgbouncer_exporter.git
PATCH=0001-pgbouncer_exporter-0.9.0-add-loongarch-support.patch
default: image

image: src/$(SOURCE)
	export ARCH=loong64
	$(MAKE) -C src/$(SOURCE) common-build
	$(MAKE) -C src/$(SOURCE) common-docker-loong64
	docker tag docker.io/prometheuscommunity/pgbouncer-exporter-linux-loong64:HEAD $(IMAGE)
src/$(SOURCE):
	git clone -b v$(TAG) --depth=1 $(SOURCE_URL) $@
	cd $@ && git apply ../../$(PATCH)
clean:
	rm -rf src
push:
	docker push $(IMAGE)
	#latest image
	@if [ $(LATEST) = "true" ]; \
		then \
		docker tag $(IMAGE) $(LATEST_IMAGE); \
		docker push $(LATEST_IMAGE); \
	fi
