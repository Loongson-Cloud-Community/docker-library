# This file is generated by the template.

REGISTRY?=lcr.loongnix.cn
ORGANIZATION?=vectordotdev
REPOSITORY?=vector
TAG?=0.49.0
LATEST?=true

IMAGE_DEBIAN:=lcr.loongnix.cn/vectordotdev/vector:0.49.0-debian

# SOURCE_URL is a url to download source, such as https://github.com/merore/merore.git.
# SOURCE is project sources, its located at src/$(SORUCE).
# PATCH is a patch that supports loong64 to $(SOURCE).
# Be sure to fill in the follows!!!
SOURCE_URL=
SOURCE=$(shell echo $(SOURCE_URL) | awk -F '/' '{print $$NF}' | awk -F '.' '{print $$1}')

default: image

#image: image-alpine image-debian
image: image-debian
# Commands for building.

image-debian: vector-0.49.0.custom.e7de56692-loongarch64-unknown-linux-gnu.tar.gz
	docker build -t $(IMAGE_DEBIAN) -f Dockerfile.debian .

vector-0.49.0.custom.e7de56692-loongarch64-unknown-linux-gnu.tar.gz:
	wget https://github.com/Loongson-Cloud-Community/vector/releases/download/0.49.0/vector-0.49.0.custom.e7de56692-loongarch64-unknown-linux-gnu.tar.gz
	tar -vxf vector-0.49.0.custom.e7de56692-loongarch64-unknown-linux-gnu.tar.gz

push:
#	docker push $(IMAGE_ALPINE) && docker push $(IMAGE_DEBIAN)
	docker push $(IMAGE_DEBIAN)

clean:
