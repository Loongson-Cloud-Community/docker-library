# This file is generated by the template.

REGISTRY	?=lcr.loongnix.cn
ORGANIZATION	?=jimmidyson
REPOSITORY	?=configmap-reload
TAG		?=0.5.0
LATEST		?=true

IMAGE=$(REGISTRY)/$(ORGANIZATION)/$(REPOSITORY):$(TAG)
LATEST_IMAGE=$(REGISTRY)/$(ORGANIZATION)/$(REPOSITORY):latest

default: image

src:
	git clone --depth 1 -b v0.5.0 https://github.com/jimmidyson/configmap-reload && cd configmap-reload && git apply ../0001-add-loong64-support.patch

image: src
	cd configmap-reload && make docker
push:
	docker push $(IMAGE)
	#latest image
	@if [ $(LATEST) = "true" ]; \
		then \
		docker tag $(IMAGE) $(LATEST_IMAGE); \
		docker push $(LATEST_IMAGE); \
	fi
