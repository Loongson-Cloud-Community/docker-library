# This file is generated by the template.

REGISTRY	?=lcr.loongnix.cn
ORGANIZATION	?=kubernetes-sigs
REPOSITORY	?=cluster-proportional-autoscaler
TAG		?=1.8.4
LATEST		?=false

IMAGE=$(REGISTRY)/$(ORGANIZATION)/$(REPOSITORY):$(TAG)
LATEST_IMAGE=$(REGISTRY)/$(ORGANIZATION)/$(REPOSITORY):latest
SOURCE=cluster-proportional-autoscaler
SOURCE_URL=https://github.com/kubernetes-sigs/cluster-proportional-autoscaler.git
PATCH=0001-support-loongarch.patch
default: image

image: src/$(SOURCE)
	$(MAKE) -C src/$(SOURCE) container ARCH=loong64
	docker tag gcr.io/k8s-staging-cpa/cluster-proportional-autoscaler-loong64:1.8.4-dirty $(IMAGE) 
src/$(SOURCE):
	git clone -b $(TAG) --depth=1 $(SOURCE_URL) $@
	cd $@ && git apply ../../$(PATCH)
clean:
	rm -rf src
push:
	docker push $(IMAGE)
	#latest image
	@if [ $(LATEST) = "true" ]; \
		then \
		docker tag $(IMAGE) $(LATEST_IMAGE); \
		docker push $(LATEST_IMAGE); \
	fi
