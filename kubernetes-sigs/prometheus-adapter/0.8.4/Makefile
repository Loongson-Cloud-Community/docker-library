# This file is generated by the template.

REGISTRY	?=lcr.loongnix.cn
ORGANIZATION	?=kubernetes-sigs
REPOSITORY	?=prometheus-adapter
TAG		?=0.8.4
LATEST		?=true

IMAGE=$(REGISTRY)/$(ORGANIZATION)/$(REPOSITORY):$(TAG)
LATEST_IMAGE=$(REGISTRY)/$(ORGANIZATION)/$(REPOSITORY):latest
SOURCE=prometheus-adapter
SOURCE_URL=https://github.com/kubernetes-sigs/prometheus-adapter.git
PATCH=0001-add-loong64-support-abi-2.0.patch
default: image

image: src/$(SOURCE)
	cd src/$(SOURCE) && $(MAKE) && $(MAKE) docker-build
	docker tag directxman12/k8s-prometheus-adapter-loong64:latest $(IMAGE) 	
src/$(SOURCE):
	git clone -b v$(TAG) --depth=1 $(SOURCE_URL) $@
	cd $@ && git apply ../../$(PATCH) && go mod tidy && go mod download
clean:
	rm -rf src 

push:
	docker push $(IMAGE)
	#latest image
	@if [ $(LATEST) = "true" ]; \
		then \
		docker tag $(IMAGE) $(LATEST_IMAGE); \
		docker push $(LATEST_IMAGE); \
	fi
