# This file is generated by the template.

REGISTRY	?=lcr.loongnix.cn
ORGANIZATION	?=kubernetes-sigs
REPOSITORY	?=metrics-server
TAG		?=0.6.2
LATEST		?=false

IMAGE=$(REGISTRY)/$(ORGANIZATION)/$(REPOSITORY):$(TAG)
LATEST_IMAGE=$(REGISTRY)/$(ORGANIZATION)/$(REPOSITORY):latest
PATCH=0001-support-loong64.patch
SOURCE=metrics-server
SOURCE_URL=https://github.com/kubernetes-sigs/metrics-server.git
default: image

image: src/$(SOURCE)
	$(MAKE) -C src/$(SOURCE) container ARCH=loong64
	docker tag gcr.io/k8s-staging-metrics-server/metrics-server-loong64:latest $(IMAGE) 
src/$(SOURCE):
	git clone -b v$(TAG) --depth=1 $(SOURCE_URL) $@
	cd $@ && git apply ../../$(PATCH)
push:
	docker push $(IMAGE)
	#latest image
	@if [ $(LATEST) = "true" ]; \
		then \
		docker tag $(IMAGE) $(LATEST_IMAGE); \
		docker push $(LATEST_IMAGE); \
	fi
