# This file is generated by the template.

REGISTRY	?=lcr.loongnix.cn
ORGANIZATION	?=bitnami
REPOSITORY	?=redis-sentinel
TAG		?=7.0.11-debian-13
COMMIT		?=5d137a78644
LATEST		?=false

IMAGE=$(REGISTRY)/$(ORGANIZATION)/$(REPOSITORY):$(TAG)
LATEST_IMAGE=$(REGISTRY)/$(ORGANIZATION)/$(REPOSITORY):latest
SOURCE_URL=https://github.com/bitnami/containers.git
SOURCE=$(shell echo $(SOURCE_URL) | awk -F '/' '{print $$NF}' | awk -F '.' '{print $$1}')
PATCH=0001-bitnami-redis-sentinel-7.0-add-loong64-support.patch

default: image

image: src/$(SOURCE)
	cd src/containers/bitnami/redis-sentinel/7.0/debian-11 && \
		docker build \
		--build-arg http_proxy=$(http_proxy) \
		--build-arg https_proxy=$(https_proxy) \
		-t $(IMAGE) .

#	git clone -b $(COMMIT) --depth 1 $(SOURCE_URL) $@
src/$(SOURCE):
	git clone https://github.com/bitnami/containers.git src/containers
	cd src/containers && git checkout 5d137a78644
	cd $@ && \
		git apply ../../$(PATCH)

push:
	docker push $(IMAGE)
	#latest image
	@if [ $(LATEST) = "true" ]; \
		then \
		docker tag $(IMAGE) $(LATEST_IMAGE); \
		docker push $(LATEST_IMAGE); \
	fi

clean:
	rm -rf src
