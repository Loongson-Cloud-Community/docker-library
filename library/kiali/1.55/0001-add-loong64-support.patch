diff --git a/deploy/docker/Dockerfile-ubi8-minimal b/deploy/docker/Dockerfile-ubi8-minimal
index 077fd5b..f85a203 100644
--- a/deploy/docker/Dockerfile-ubi8-minimal
+++ b/deploy/docker/Dockerfile-ubi8-minimal
@@ -1,16 +1,16 @@
-FROM registry.access.redhat.com/ubi8-minimal
+FROM lcr.loongnix.cn/library/alpine:3.19
 
-LABEL maintainer="kiali-dev@googlegroups.com"
+LABEL maintainer="wanghao03@loongson.cn"
 
 ENV KIALI_HOME=/opt/kiali \
     PATH=$KIALI_HOME:$PATH
 
 WORKDIR $KIALI_HOME
 
-RUN microdnf install -y shadow-utils && \
-    microdnf clean all && \
+RUN apk add shadow && \
+    apk cache clean && \
     rm -rf /var/cache/yum && \
-    adduser --uid 1000 kiali
+    adduser --uid 1000 --disabled-password kiali
 
 COPY kiali $KIALI_HOME/
 
diff --git a/deploy/get-console.sh b/deploy/get-console.sh
index 287e1f9..9189357 100755
--- a/deploy/get-console.sh
+++ b/deploy/get-console.sh
@@ -13,20 +13,23 @@ CONSOLE_DIR=${CONSOLE_LOCAL_DIR:-$DIR/frontend}
 mkdir -p $DIR/_output/docker
 
 # Some sanity checks of CONSOLE_DIR. Some checks are naive.
-if [ -z "$CONSOLE_DIR" ]; then
-  echo "You must set the CONSOLE_LOCAL_DIR environment variable to the path where the UI source code is located."
-  exit 1
-elif [ ! -f "$CONSOLE_DIR/package.json" ]; then
-  echo "CONSOLE_DIR is $CONSOLE_DIR"
-  echo "Apparently, this CONSOLE_DIR does not contain the kiali UI"
-  exit 1
-elif [ ! -d "$CONSOLE_DIR/build" ] || [ -z "$(ls -A $CONSOLE_DIR/build)" ]; then
-  echo "CONSOLE_DIR is $CONSOLE_DIR"
-  echo "Apparently, the kiali UI is not built."
-  echo "Build the front-end by running 'yarn && yarn build' inside the kiali UI directory"
-  exit 1
-fi
+#if [ -z "$CONSOLE_DIR" ]; then
+#  echo "You must set the CONSOLE_LOCAL_DIR environment variable to the path where the UI source code is located."
+#  exit 1
+#elif [ ! -f "$CONSOLE_DIR/package.json" ]; then
+#  echo "CONSOLE_DIR is $CONSOLE_DIR"
+#  echo "Apparently, this CONSOLE_DIR does not contain the kiali UI"
+#  exit 1
+#elif [ ! -d "$CONSOLE_DIR/build" ] || [ -z "$(ls -A $CONSOLE_DIR/build)" ]; then
+#  echo "CONSOLE_DIR is $CONSOLE_DIR"
+#  echo "Apparently, the kiali UI is not built."
+#  echo "Build the front-end by running 'yarn && yarn build' inside the kiali UI directory"
+#  exit 1
+#fi
 
-echo "Copying local console files from $CONSOLE_DIR"
+echo "Copying local console files from https://github.com/Loongson-Cloud-Community/kiali/releases/download/v1.55.0/console.tar.gz"
 rm -rf $DIR/_output/docker/console && mkdir $DIR/_output/docker/console
-cp -r $CONSOLE_DIR/build/* $DIR/_output/docker/console
+wget https://github.com/Loongson-Cloud-Community/kiali/releases/download/v1.55.0/console.tar.gz
+tar -zxvf console.tar.gz
+cp -r console/* $DIR/_output/docker/console
+#rm -rf $DIR/_output/docker/tmp
diff --git a/make/Makefile.container.mk b/make/Makefile.container.mk
index 8b6bd50..5a8908e 100644
--- a/make/Makefile.container.mk
+++ b/make/Makefile.container.mk
@@ -14,7 +14,7 @@
 container-build-kiali: .prepare-kiali-image-files
 ifeq ($(DORP),docker)
 	@echo Building container image for Kiali using docker
-	docker build --pull -t ${QUAY_TAG} -f ${OUTDIR}/docker/${KIALI_DOCKER_FILE} ${OUTDIR}/docker
+	docker build  -t lcr.loongnix.cn/library/kiali:v1.55 -f ${OUTDIR}/docker/${KIALI_DOCKER_FILE} ${OUTDIR}/docker
 else
 	@echo Building container image for Kiali using podman
 	podman build --pull -t ${QUAY_TAG} -f ${OUTDIR}/docker/${KIALI_DOCKER_FILE} ${OUTDIR}/docker
