# This file is generated by the template.

REGISTRY	?=lcr.loongnix.cn
ORGANIZATION	?=library
REPOSITORY	?=kibana
TAG		?=8.11.0
LATEST		?=true

IMAGE=$(REGISTRY)/$(ORGANIZATION)/$(REPOSITORY):$(TAG)
LATEST_IMAGE=$(REGISTRY)/$(ORGANIZATION)/$(REPOSITORY):latest

default: image
SOURCE=kibana
SOURCE_URL=https://github.com/Loongson-Cloud-Community/kibana/releases/download/v8.11.0/kibana-8.11.0-linux-loongarch64-abi2.0.tar.gz

image: kibana-8.11.0-linux-loongarch64-abi2.0.tar.gz
	docker build \
		--build-arg http_proxy=$(http_proxy) \
		--build-arg https_proxy=$(https_proxy) \
		-t $(IMAGE) \
		.
kibana-8.11.0-linux-loongarch64-abi2.0.tar.gz:
	wget $(SOURCE_URL)
	tar -vxf kibana-8.11.0-linux-loongarch64-abi2.0.tar.gz 
clean:
	rm -rf kibana-8.11.0 kibana-8.11.0-linux-loongarch64-abi2.0.tar.gz
push:
	docker push $(IMAGE)
	#latest image
	@if [ $(LATEST) = "true" ]; \
		then \
		docker tag $(IMAGE) $(LATEST_IMAGE); \
		docker push $(LATEST_IMAGE); \
	fi
