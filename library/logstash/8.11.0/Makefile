# This file is generated by the template.

REGISTRY	?=lcr.loongnix.cn
ORGANIZATION	?=library
REPOSITORY	?=logstash
TAG		?=8.11.0
LATEST		?=true

IMAGE=$(REGISTRY)/$(ORGANIZATION)/$(REPOSITORY):$(TAG)
LATEST_IMAGE=$(REGISTRY)/$(ORGANIZATION)/$(REPOSITORY):latest

default: image
SOURCE=logstash
SOURCE_URL=https://github.com/Loongson-Cloud-Community/logstash/releases/download/v8.11.0/logstash-8.11.0-linux-loongarch64-abi2.0.tar.gz

image: logstash-8.11.0-linux-loongarch64-abi2.0.tar.gz
	docker build \
		--build-arg http_proxy=$(http_proxy) \
		--build-arg https_proxy=$(https_proxy) \
		-t $(IMAGE) \
		.
logstash-8.11.0-linux-loongarch64-abi2.0.tar.gz:
	wget $(SOURCE_URL)
	tar -vxf logstash-8.11.0-linux-loongarch64-abi2.0.tar.gz	
clean:
	rm -rf logstash-8.11.0-linux-loongarch64-abi2.0.tar.gz logstash-8.11.0
push:
	docker push $(IMAGE)
	#latest image
	@if [ $(LATEST) = "true" ]; \
		then \
		docker tag $(IMAGE) $(LATEST_IMAGE); \
		docker push $(LATEST_IMAGE); \
	fi
