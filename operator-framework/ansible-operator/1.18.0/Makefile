# This file is generated by the template.

REGISTRY?=lcr.loongnix.cn
ORGANIZATION?=operator-framework
REPOSITORY?=ansible-operator
TAG?=1.18.0

IMAGE=$(REGISTRY)/$(ORGANIZATION)/$(REPOSITORY):$(TAG)


default: image

image:
	docker build \
		-t $(IMAGE) \
		.

push:
	docker push $(IMAGE)

