# This file is generated by the template.

REGISTRY?=lcr.loongnix.cn
ORGANIZATION?=kubevirt
REPOSITORY?=virt-handler
TAG?=0.50.0

IMAGE=$(REGISTRY)/$(ORGANIZATION)/$(REPOSITORY):$(TAG)


default: image

download: clean
	wget "http://cloud.loongnix.cn/releases/loongarch64/kubevirt/virt-handler/0.50.0/virt-handler"
	chmod 755 virt-handler
	wget "http://cloud.loongnix.cn/releases/loongarch64/kubevirt/virt-chroot/0.50.0/virt-chroot"
	chmod 755 virt-chroot
	wget "http://cloud.loongnix.cn/releases/loongarch64/kubevirt/container-disk/0.50.0/container-disk"
	chmod 755 container-disk

image: download
	docker build \
		-t $(IMAGE) \
		.

push:
	docker push $(IMAGE)

clean:
	rm -f virt-handler \
		virt-chroot \
		container-disk
